from setuptools import setup, find_packages

setup(
    name='stb_amount',
    version='1.0.0',
    description='stabilize add, update and delete',
    long_description='stabilize add, update and delete',
    author='ReimiBeta',
    author_email='reimi846@gmail.com',
    url='https://github.com/reimibeta/stb_amount',
    license='MIT',
    packages=find_packages(),
    # py_modules=['image_compress',],
    install_requires=[
        # other dependencies
    ],
    # other optional arguments
)
